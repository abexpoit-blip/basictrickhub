# Admin panel password
ADMIN_PASSWORD=basictrick-admin

# Optional: also set Telegram bot in Admin → Settings UI (method notify)
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=

# AI Telegram bot + payments are configured in Admin → Telegram Admin UI
# (stored in data/telegram-bot.json — bot token, ZiniPay, Plisio, webhook secret)
# Public HTTPS URL required for webhook (ngrok / production domain)
# Example webhook: https://YOUR_DOMAIN/api/telegram/webhook?secret=YOUR_SECRET
